1. 指针间接操作的三个必要条件。
条件1 2个变量 （通常一个实参，一个形参）
条件2 建立关系，实参取地址赋给形参指针
条件3 *p 形参去间接修改实参的值

2. 简述sizeof和strlen的区别
sizeof是一个操作符符 计算数据类型的大小
strlen是库函数 计算 数据类型的长度

3. 是不是程序上所有的内存空间都允许我们程序使用？
   想象访问程序的非法空间的后果？
   何为野指针？
   如何避免使用非法空间和野指针？

  不是。
 访问非法空间的值是不可以估量的。
 野指针  是指向了一个未知未定义的地址，或者是指向了一个非法空间。
 

4. char str[] = "12345", 为str所指向的内存空间开辟的多少字节？
6个字节 \0

5. 基于第4题 strlen(str) 和 sizeof(str) 分别是多少？为什么？ strlen = 5   sizeof =6
   如果 char *str2 = "12345", 那么 strlen(str2) 和 sizeof(str2)又是多少？
  strlen = 5   sizeof = 4
   为什么？


6. 何为输出参数？意义何在？

7. 练习C标准库 相关字符串接口。